% LaTeX equation source for 34_tust_manuscript_working_draft_en_review_copy.docx
% The DOCX uses rendered equation images to avoid cross-platform font corruption.
% Each image stores the corresponding LaTeX source as alternative text.

\begin{equation}
\rho:E\rightarrow\mathcal{R}\cup\{\varnothing\}
\end{equation}

\begin{equation}
v(k)=
\begin{cases}
1.427, & k\leq0.2,\\
\max(1.427-0.3549k,0), & 0.2<k\leq4.0,\\
0, & k>4.0.
\end{cases}
\end{equation}

\begin{equation}
c^{\mathrm{Imp}}_e(t)=\alpha l_e+\beta\,t^{\mathrm{move}}_e(t),
\qquad h(n)=\gamma d(n,\mathcal{X})
\end{equation}

\begin{equation}
\widehat{Q}_r(\tau)=\max\left\{0,\;Q_r(t)+A_r(t,\tau)-\mu_r(\tau-t)\right\}
\end{equation}

\begin{equation}
w_r(\tau)=\widehat{Q}_r(\tau)/\mu_r
\end{equation}

\begin{equation}
\tau_j=\tau_i+w_{\rho(e)}(\tau_i)+s^{\mathrm{batch}}_{\rho(e)}
+t^{\mathrm{move}}_e(\tau_i)+w^{\mathrm{space}}_j(\tau_j)
\end{equation}

\begin{equation}
L_1\prec L_2
\Longleftrightarrow
\tau_1\leq\tau_2\;\land\;g_1\leq g_2
\end{equation}

\begin{equation}
T_p=\min\{t:E^{\mathrm{out}}(t)\geq pN\},
\qquad p\in\{0.50,0.80,0.90,0.95,0.99,1.00\}
\end{equation}

\begin{equation}
W^{\mathrm{stat}}=\sum_t N^{\mathrm{stationary}}(t)\,\Delta t
\end{equation}
