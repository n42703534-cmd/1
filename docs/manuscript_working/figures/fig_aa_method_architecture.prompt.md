# Generation prompt — `fig_aa_method_architecture.png`

Mode: built-in `image_gen`; use case: `infographic-diagram`.

Image 1 is a visual style reference only, not an edit target. Create a brand-new wide 16:9 scientific framework diagram for an arrival-time-aware queue-sensitive metro evacuation method. Use exactly three vertical columns: left inputs, center prediction–execution loop, and right continuous-space evaluation. Use a pure white background, thin navy-gray lines, restrained pale blue/lavender/apricot/teal/sage fills, large crisp sans-serif English labels, flat vector icons, generous whitespace, no texture, no shadows, no gradients, no 3D, no photographs, no watermark, and no numerical experimental results.

The left panel is titled `INPUTS & NETWORK ABSTRACTION` and contains `Station Topology`, `Directed Graph  G = (N, E)`, `Demand Scenarios / Reference + Train Arrivals`, `Shared Resources / Stairs • Escalators • Gates`, plus parameter chips `Service Rate  μr`, `Receiving Capacity  Kj`, and `Speed–Density  v(ρ)`. Feed the inputs separately into the upper planning and lower execution modules.

The center panel is titled `PREDICTION–EXECUTION LOOP`. At the top, include a static, unconnected `CONTROLLED COMPARISON` strip with `Improved A* / Current Queue`, `Proposed AA* / Queue at ETA`, and `Same Physical Executor`. Below it, create an `ARRIVAL-TIME-AWARE AA*` sequence: `Candidate Paths` → `ETA Propagation` → `Committed Arrival Events` → `Queue at ETA  Q̂r(τ)` → `Multi-label A*`. Add a `TIME-DEPENDENT PATH COST` strip with `Movement`, `Predicted Queue`, `Batch Service`, `Spatial Receiving`, and `Density Exposure`. Below that, show `Route Assignment / Rolling Replanning`, followed by a downward `Accepted Requests` arrow into `DYNAMIC NETWORK-FLOW EXECUTION`, containing `Shared-Resource Mapping`, `Capacity Allocation`, `Receiving Constraint`, `Density–Speed Update`, `In-Transit Events`, and a lock badge `In-Transit Route Locked`. Draw a dominant internal feedback loop from execution back to planning, labeled `Occupancy • Queues • Future Arrivals`.

The right panel is titled `CONTINUOUS-SPACE EVALUATION` and contains `Export Route & Exit Allocation`, `Pathfinder Execution`, a `NETWORK LAYER` metric card (`Mean • T95 • T100`, `Stationary Exposure`, `Distance • Load Balance`), a `CONTINUOUS SPACE` metric card (`Completion Time`, `Congestion Time`, `Walking Distance`), and `CROSS-MODEL ASSESSMENT`. The central-to-right process flow is one-way only: route assignment feeds continuous-space evaluation; Pathfinder never feeds back to AA*.
